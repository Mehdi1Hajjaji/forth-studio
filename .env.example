############################################################
# Example environment variables
# Copy to .env (for local dev) and set real values.
# For Vercel, set these in the Project Settings per environment.
############################################################

# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DB?pgbouncer=true&connection_limit=1&sslmode=require"

# App URLs
# Development (local): keep auth + app URLs on localhost
NEXTAUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Production (Vercel): set these in Vercel env (DON'T keep in local .env)
# NEXTAUTH_URL="https://your-app.vercel.app"  # or your custom domain
# NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"

# NextAuth
NEXTAUTH_SECRET="generate_with: openssl rand -base64 32"

# OAuth (optional for future providers)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# Email (password reset)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="no-reply@your-domain.com"

# LiveKit: required for Code & Cry
LIVEKIT_URL="wss://your-livekit.livekit.cloud"
LIVEKIT_API_KEY=""
LIVEKIT_API_SECRET=""

# Notes for OAuth setup (Production)
# - Set these in Vercel, not in the committed .env
# - Callback URLs (must exactly match NEXTAUTH_URL):
#   GitHub: <NEXTAUTH_URL>/api/auth/callback/github
#   Google: <NEXTAUTH_URL>/api/auth/callback/google
